#ifndef BUTTONS_H
#define BUTTONS_H

// Call once from app_main, AFTER ui_init() has run
// (so objects.next / objects.ok / objects.gardennext / objects.gardenok exist)
void buttons_init(void);

#endif
