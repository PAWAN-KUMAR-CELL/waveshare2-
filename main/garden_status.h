#pragma once

void garden_status_init(void);   // call once in app_main
void garden_status_start(void);  // call when garden screen becomes active
void garden_status_stop(void);   // call when garden screen becomes inactive